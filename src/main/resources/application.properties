spring.application.name=crawler
server.port=5000

server.tomcat.max-connections=100
server.tomcat.threads.max=10

# Jsoup connect + read timeout per URL (ms). EC2 and other cloud egress can be slower than a home ISP; raise if you see "Read timed out".
crawler.http.timeout-ms=30000
